{
  "buildCommand": "npm run build && npm install",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/handler.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
